// Shared localStorage keys for the carousel template/palette selection —
// previously duplicated as separate string literals in Create.tsx and
// Result.tsx, which risked drifting out of sync if either ever changed.
export const CAROUSEL_TEMPLATE_KEY = 'ca_carousel_template_id';
export const CAROUSEL_PALETTE_KEY = 'ca_carousel_palette_id';
